# RADS Python dependencies
#
# Default install:
#   pip install -r requirements.txt
#
# This single environment covers:
# - EDA and metadata analysis
# - existing ResNet18 + GRU baseline training/evaluation
# - Phase 1+ YOLO/object-centric RADS pipeline work
#
# For GPU training, install the PyTorch build that matches your CUDA version
# first if the default pip wheels are not appropriate for your machine:
#   https://pytorch.org/get-started/locally/

# Core data science
pandas>=2.0.0
numpy>=1.24.0

# Visualization and reporting
matplotlib>=3.7.0
seaborn>=0.13.0
tabulate>=0.9.0

# Configuration
pyyaml>=6.0

# Experiment tracking
wandb>=0.16.0

# Jupyter / notebooks
jupyter>=1.0.0
ipykernel>=6.0.0
nbformat>=5.0.0

# System monitoring
psutil>=5.9.0
gputil>=1.4.0

# Video processing
opencv-python>=4.8.0

# Training and evaluation
torch>=2.0.0
torchvision>=0.15.0
scikit-learn>=1.3.0
tqdm>=4.65.0

# Object-centric RADS pipeline
ultralytics>=8.0.0
